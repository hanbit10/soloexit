import { Card, CardContent, CardHeader } from "@/components/ui/Card";

export function RoutineChart() {
  return (
    <Card>
      <CardHeader> Chart is this showing</CardHeader>
      <CardContent>Card Content</CardContent>
    </Card>
  );
}
